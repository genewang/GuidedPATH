apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: guidedpath-frontend
spec:
  selector:
    app: guidedpath-frontend
  ports:
  - name: http
    port: 80
    targetPort: 3000
  type: ClusterIP
