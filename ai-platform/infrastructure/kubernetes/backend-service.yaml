apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: guidedpath-backend
spec:
  selector:
    app: guidedpath-backend
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP
